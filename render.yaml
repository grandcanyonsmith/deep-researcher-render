services:
  - type: web
    name: deep-researcher-service
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    healthCheckPath: /research?topic=ping
    envVars:
      - key: OPENAI_API_KEY
        sync: false
